[tool.poetry]
name = "calculator-api"
version = "0.1.0"
description = "A simple calculator API for learning FastAPI and cloud deployment"
authors = ["Max Chen <chen3805@purdue.edu>"]
package-mode = false

[tool.poetry.dependencies]
python = "^3.11"
fastapi = "^0.109.0"
uvicorn = {extras = ["standard"], version = "^0.27.0"}
google-cloud-bigquery = "^3.40.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
